<p style="color: green"><%= notice %></p>

<h1>Ползватели на СПО, с които имате действащ договор</h1>

<table>
  <thead>
    <tr>
      <th>Име</th>
      <th>ЕИК</th>
      <th>Адрес</th>
      <th>Електронна поща</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% Company.all.each do |company| %>
      <tr>
        <% if company[:contractors].include?(Current.user[:company_number].to_s) %>
          <td><%= company.name %></td>
          <td><%= company.uic %></td>
          <td><%= company.address %></td>
          <td><%= company.email %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
